(function print(){
    console.log("Hello world")
}
)();

/* IIFE (Immediately invoked function expression  is executed immediately after it's creation
variable defined IIFE ar enot accessible from outside it's scope which help's to avoid conflict with
other variable's or functions.IIFE keeps variables or functions private in it.*/  